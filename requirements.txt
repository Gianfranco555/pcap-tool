# ---- runtime dependencies for pcap_tool parser MVP ----
pandas~=2.2        # dataframe handling & CSV/JSON export
pyshark~=0.6       # wraps the tshark CLI to read .pcap/.pcapng
scapy~=2.5         # packet dissection for protocols pyshark can’t decode
tqdm~=4.66         # progress bars while parsing large captures
tabulate~=0.9
